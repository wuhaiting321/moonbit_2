# Native benchmark — 2026-09-16

This record was generated locally from the repository benchmark package. It is
a parser throughput measurement, not a claim about every Wasm input.

## Command

```bash
moon run benchmarks --target native --release
```

## Workload

- Operation 1: `WasmModule::parse`
- Operation 1 input: deterministic 14-byte Wasm module with one type section
  and no function bodies
- Operation 1 iterations per run: 10,000
- Operation 2: `WasmModule::parse` plus `validate`
- Operation 2 input: deterministic 28-byte Wasm module with type, function,
  export, and code sections (one empty function exported as "main")
- Operation 2 iterations per run: 1,000
- Every iteration completed successfully in all recorded runs.

## Environment

- OS: Windows NT 10.0.26200.0
- Moon: 0.1.20260916
- Moon compiler: v0.10.9+6e6c44045
- CPU model: not available because the local WMI query was denied by the
  operating-system permission profile

## Three local runs

| Run | Parse elapsed | Parse operations/second | Validate elapsed | Validate operations/second |
| ---: | ---: | ---: | ---: | ---: |
| 1 | 8 ms | 1,250,000 | 10 ms | 100,000 |
| 2 | 10 ms | 1,000,000 | 12 ms | 83,333 |
| 3 | 12 ms | 833,333 | 14 ms | 71,428 |

Median parse result: 10 ms for 10,000 successful parses, or 1,000,000
operations/second. Median validate result: 12 ms for 1,000 parse+validate
cycles, or 83,333 operations/second. These workloads are intentionally
deterministic and small; re-run the command when comparing changes because
timings are machine- and load-dependent.
