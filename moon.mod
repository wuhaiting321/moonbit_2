name = "wuhaiting321/wasmkit"

version = "0.1.0"

readme = "README.md"

repository = "https://github.com/wuhaiting321/moonbit-wasmkit"

license = "Apache-2.0"

keywords = [ "wasm", "webassembly", "parser", "validator", "binary" ]

description = "MoonBit WebAssembly binary parser, section decoder, bytecode disassembler, module validator, and analysis toolkit."

import {
  "moonbitlang/x@0.4.48",
}
